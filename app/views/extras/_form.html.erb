<%= form_for(@extra, builder: ThemeBuilder) do |f| %>
  <% if @extra.errors.any? %>
    <hr/>
    <div class="has-error">
      <ul>
      <% @extra.errors.full_messages.each do |message| %>
        <li><i class="input-helper"><%= message %></i></li>
      <% end %>
      </ul>
    </div>
    <hr/>
  <% end %>

    <%= f.text_field :name, label: 'Name'%>
    <%= f.submit 'Enviar'%>
<% end %>
