<% all_zones =  Zone.order('parent_zone_id Desc') %>



<%= card  ['Acuerdo: Zonas', "Zonas asignadas: #{zones.count}. Haga click para agregar y eliminar"] do %>
  <%= tree :tree2, all_zones, :name, :id, :zones, :parent_zone_id, link, zones.pluck(:id) %>
  <%# tree_node all_zones.last, :name, :id, :zones %>
<% end %>
