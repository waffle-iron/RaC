<% toolbar = Theme::Toolbar.new %>
<% toolbar.add_button 'Nuevo', new_place_type_path, 'plus' %>
<% toolbar.add_button 'Ver', place_type_path(@place_type), 'eye' %>
<% toolbar.add_button 'Lista', place_types_path, 'view-list-alt' %>
<% toolbar.add_button 'Eliminar', place_type_path(@place_type), 'delete' %>

<%= render '/layouts/block_header' %>

<%= card ['Place Types', 'Editar'], toolbar do %>

<%= render 'form' %>

<% end %>
