<%= form_for(@place_type, builder: ThemeBuilder) do |f| %>
  <% if @place_type.errors.any? %>
    <hr/>
    <div class="has-error">
      <ul>
      <% @place_type.errors.full_messages.each do |message| %>
        <li><i class="input-helper"><%= message %></i></li>
      <% end %>
      </ul>
    </div>
    <hr/>
  <% end %>

    <%= f.text_field :name, label: 'Name'%>
    <%= f.submit 'Enviar'%>
<% end %>
