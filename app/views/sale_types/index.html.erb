<% toolbar = Theme::Toolbar.new %>
<% toolbar.add_button 'Nuevo', new_sale_type_path, 'plus' %>
<% toolbar.add_button 'Lista', sale_types_path, 'view-list-alt' %>

<%= render '/layouts/block_header' %>

<%= card ['Tipos de Venta', 'Listado'], toolbar do %>
  <script type="text/javascript">
   var commands = function(column, row) {
     var show_command = '<%= table_button 'Ver', sale_type_path('rowId'), 'eye' %>';
     var edit_command = '<%= table_button 'Editar', edit_sale_type_path('rowId'), 'edit' %>';
     var delete_command = '<%= table_button 'Eliminar', sale_type_path('rowId'), 'delete', {'redirect_to' => 'sale_types'}, 'command-delete' %>';

     return (show_command + edit_command + delete_command).replace(new RegExp('rowId', 'g'), row.id);
     };
  </script>
  <table id="data-table-command" class="table table-striped table-vmiddle">
    <thead>
      <tr>
        <th data-column-id="id" data-type="numeric">Id</th>

        <th data-column-id="name" >
          Nombre
        </th>

        <th data-column-id="commands" data-formatter="commands" data-sortable="false">Commands</th>
      </tr>
    </thead>
    <tbody>
      <% @sale_types.each do |sale_type| %>
      <tr>
        <td><%= sale_type.id %></td>

        <td><%= sale_type.name %></td>

        <td>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
<% end %>
