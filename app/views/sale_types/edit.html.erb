<% toolbar = Theme::Toolbar.new %>
<% toolbar.add_button 'Nuevo', new_sale_type_path, 'plus' %>
<% toolbar.add_button 'Ver', sale_type_path(@sale_type), 'eye' %>
<% toolbar.add_button 'Lista', sale_types_path, 'view-list-alt' %>
<% toolbar.add_button 'Eliminar', sale_type_path(@sale_type), 'delete' %>

<%= render '/layouts/block_header' %>

<%= card ['Tipos de Venta', 'Editar'], toolbar do %>

<%= render 'form' %>

<% end %>
