<% toolbar = Theme::Toolbar.new %>
<% toolbar.add_button 'Nuevo', new_company_type_path, 'plus' %>
<% toolbar.add_button 'Lista', company_types_path, 'view-list-alt' %>

<%= render '/layouts/block_header' %>

<%= card ['Company Types', 'Listado'], toolbar do %>
  <script type="text/javascript">
   var commands = function(column, row) {
     var show_command = '<%= table_button 'Ver', company_type_path('rowId'), 'eye' %>';
     var edit_command = '<%= table_button 'Editar', edit_company_type_path('rowId'), 'edit' %>';
     var delete_command = '<%= table_button 'Eliminar', company_type_path('rowId'), 'delete', {'redirect_to' => 'company_types'}, 'command-delete' %>';

     return (show_command + edit_command + delete_command).replace(new RegExp('rowId', 'g'), row.id);
     };
  </script>
  <table id="data-table-command" class="table table-striped table-vmiddle">
    <thead>
      <tr>
        <th data-column-id="id" data-type="numeric">Id</th>

        <th data-column-id="name" >
          Name
        </th>

        <th data-column-id="commands" data-formatter="commands" data-sortable="false">Commands</th>
      </tr>
    </thead>
    <tbody>
      <% @company_types.each do |company_type| %>
      <tr>
        <td><%= company_type.id %></td>

        <td><%= company_type.name %></td>

        <td>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
<% end %>
