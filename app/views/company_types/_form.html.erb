<%= form_for(@company_type, builder: ThemeBuilder) do |f| %>
  <% if @company_type.errors.any? %>
    <hr/>
    <div class="has-error">
      <ul>
      <% @company_type.errors.full_messages.each do |message| %>
        <li><i class="input-helper"><%= message %></i></li>
      <% end %>
      </ul>
    </div>
    <hr/>
  <% end %>

    <%= f.text_field :name, label: 'Name'%>
    <%= f.submit 'Enviar'%>
<% end %>
