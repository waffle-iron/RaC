<%= form_for(@type_category, builder: ThemeBuilder) do |f| %>
  <% if @type_category.errors.any? %>
    <hr/>
    <div class="has-error">
      <ul>
      <% @type_category.errors.full_messages.each do |message| %>
        <li><i class="input-helper"><%= message %></i></li>
      <% end %>
      </ul>
    </div>
    <hr/>
  <% end %>

    <%= f.text_field :name, label: 'Name'%>
    <%= f.submit 'Enviar'%>
<% end %>
